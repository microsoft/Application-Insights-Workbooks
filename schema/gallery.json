{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Microsoft/Application-Insights-Workbooks/blob/master/schema/gallery.json",
  "type": "object",
  "description": "Workbook template gallery schema",
  "required": [
    "$schema",
    "categories"
  ],
  "properties": {
    "$schema": {
      "$id": "#/$schema",
      "type": "string",
      "default": "https://github.com/Microsoft/Application-Insights-Workbooks/blob/master/schema/gallery.json"
    },
    "categories": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "$id": "#/categories/id",
            "type": "string",
            "description": "ID for the category"
          },
          "description": {
            "$id": "#/categories/description",
            "type": "string",
            "description": "Localized description for the category"
          },
          "name": {
            "$id": "#/categories/name",
            "type": "string",
            "description": "Localized name for the category"
          },
          "templates": {
            "type": "array",
            "items": [
              {
                "type": "object",
                "properties": {
                  "id": {
                    "$id": "#/categories/templates/id",
                    "type": "string",
                    "description": "The folder where the template resides relative to the root repository",
                    "examples": [
                      "Workbooks/Performance/Apdex"
                    ]
                  },
                  "author": {
                    "$id": "#/categories/templates/author",
                    "type": "string",
                    "description": "Author of the template",
                    "examples": [
                      "Microsoft"
                    ]
                  },
                  "name": {
                    "$id": "#/categories/templates/name",
                    "type": "string",
                    "description": "Localized name for the template"
                  },
                  "description": {
                    "$id": "#/categories/templates/description",
                    "type": "string",
                    "description": "Localized description for the template"
                  },
                  "isPreview": {
                    "$id": "#/categories/templates/ispreview",
                    "type": "boolean",
                    "description": "Flag to mark the template as preview"
                  }
                },
                "required": [
                  "id",
                  "author",
                  "name"
                ]
              }
            ]
          }
        },
        "required": [
          "id",
          "name",
          "templates"
        ]
      }
    }
  }
}