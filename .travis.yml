language: node_js
node_js: "stable"
notifications:
  email: true
if: (type = push AND branch = master) OR (type = pull_request)
jobs:
  include:
  - os: linux
    language: node_js    
  - os: windows
    language: generic
    scripts: $TRAVIS_BUILD_DIR/scripts/processTemplates.cmd
